name: Run tests
on:
  push:
    branches:
      - feature/test-setup
  pull_request:
    branches:
      - dev
      - main
jobs:
  run-tests:
    runs-on: ubuntu-latest
    name: Run tests
    steps:
      - uses: actions/checkout@v3
      - run: npm install
      - run: npm run test
